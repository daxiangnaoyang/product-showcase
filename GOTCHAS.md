# Product Showcase GOTCHAS

只记录会改变结果的真实坑位；通用提醒不在这里重复。

## 1. 特定 Agent 的专用工具名不能直接迁移

- 现象：原 Skill 直接写入某个 Agent 的下载、读取、图片编辑、视频生成和文件展示工具。
- 原因：工具名和调用协议不是所有 Agent 共有能力。
- 处理：主入口只描述能力和降级路径；运行时先确认实际工具，再调用对应适配 Skill。工具不可用时交付 Prompt、分镜和素材缺口。

## 2. 产品图不能证明不可见参数

- 现象：从包装、外观或宣传图推断成分、材质等级、续航、认证、功效或价格。
- 原因：图片只提供可见证据，不能替代用户事实或可审计来源。
- 处理：事实卡分开记录“已提供”和“图中可见”；看不见的字段保留为“未获取/待补充”。

## 3. 图像模型容易改动产品身份

- 现象：生成后的轮廓、颜色、接口、Logo、包装文字或数字与原产品不一致。
- 原因：把产品图和无关参考图混用，或只给文字 Prompt 而没有锁定产品参考图。
- 处理：每个编辑任务只绑定必要的产品参考图；生成后逐项回读身份、文字和数字，失败就重做，不用后期裁切掩盖。

## 4. 脚本不是成片

- 现象：已有分镜或视频 Prompt，却被描述成“视频已完成”。
- 原因：没有区分脚本、生成任务、预览和最终文件。
- 处理：分别记录状态；只有存在可回读的真实输出文件时才标记成片，否则写“未生成/未验证”。

## 5. 默认视觉风格过拟合

- 现象：所有产品都套用深色大片风、同一比例或同一品牌插画风。
- 原因：示例风格被误当成工作流硬约束。
- 处理：先看产品品类、平台和用户偏好；默认只固定信息结构（一图一卖点、文字可读、身份稳定），不固定装饰风格。
