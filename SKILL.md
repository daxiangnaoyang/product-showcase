---
name: product-showcase
description: "Use when the user asks for 产品介绍、产品说明、产品视频、产品图文、推荐语、种草文案、朋友圈分享，或要把产品图片与文字说明转成事实有边界的推荐文案、电商卖点图解、朋友圈短文/配图和 15 秒产品卖点视频脚本或成片。"
version: 2.0.0
author: Hermes Agent
license: MIT
type: workflow
metadata:
  hermes:
    tags: [product, showcase, ecommerce, copywriting, social-post, short-video, image-editing]
    related_skills: [video-director, workflow-video-production, baoyu-infographic, xiaohei-elephant-scenes]
triggers:
  - 产品介绍
  - 产品说明
  - 产品视频
  - 产品图文
  - 产品推荐语
  - 推荐语
  - 种草文案
  - 朋友圈分享
  - 产品卖点
---

# 产品展示工作流（Product Showcase）

## Overview

把一款产品的图片与文字说明编译成一套可审阅、可迭代的内容包：

产品素材 → 产品事实卡 → 推荐文案 → 电商卖点图解 → 朋友圈短文与配图 → 15 秒卖点视频脚本/成片。

这是跨 Agent 的工作流。图片检查、图像生成/编辑和视频生成只调用当前运行时实际提供的能力；没有对应工具时，交付可执行的 Prompt、分镜和素材缺口，不声称已经生成文件或成片。

## When to Use

当用户提出以下任一意图时触发：

- 产品介绍、产品说明、产品卖点提炼、产品推荐语、产品种草。
- 产品图文、产品详情图、电商图解图、产品配图。
- 朋友圈分享短文、产品宣传短文、社交平台产品文案。
- 产品视频、产品宣传短片、15 秒产品卖点浓缩介绍。
- 把已有产品图和文字说明改造成多种传播素材。

不要把本 Skill 用于：

- 只有品牌定位、产品命名或长期营销策略、但暂时没有具体产品素材的任务。
- 需要独立事实调研、竞品价格监测或法规意见的任务；这些只能作为本流程的外部输入，不能用本 Skill 补造结论。
- 用户明确只要一句话改写且不需要产品素材整理时；直接完成局部改写即可。

## 交付边界

默认准备三类产物；用户只指定其中一类时，只做指定范围：

1. **推荐语 + 电商卖点图解**
   - 1—3 句推荐语。
   - 按核心卖点拆分的 2—5 张图解候选；默认一图一卖点，比例按平台选择 3:4、4:5 或 9:16。
   - 以用户提供的产品图为视觉依据进行重构或编辑，不能把无关产品当成替代品。
2. **朋友圈分享短文 + 配图**
   - 1—2 版短文；默认约 80—200 字，按平台和用户要求调整。
   - 1—3 张主图、细节图或使用场景图；图像比例按投放位置适配。
3. **15 秒产品卖点浓缩介绍**
   - 精确 15 秒的分镜、口播、字幕、画面、素材引用和节奏说明。
   - 当前运行时支持视频生成且用户需要成片时，再生成视频；否则交付脚本和生产 Prompt。

本流程只准备内容和本地产物，不默认发送、发布、投放、私聊或承诺成交。外部发布必须另有明确授权。

## 核心规则

### 1. 先建产品事实卡，不直接写成稿

第一轮用 references/collection-template.md 组织一次结构化收集，避免零碎追问。至少确认：

- 产品名、品类、目标人群、使用场景。
- 2—5 个核心卖点及其优先级。
- 用户提供的价格、规格、材质、成分、认证、售后或优惠信息。
- 产品图片、细节图、使用场景图、品牌资产及其使用边界。
- 目标平台、文案口吻、图片比例、视频方向和是否需要真人出镜。

把每条信息标为以下一种状态：

| 状态 | 含义 | 可否直接写入公开产物 |
|---|---|---|
| 已提供 | 用户明确给出的文字或文件事实 | 可以 |
| 图中可见 | 图片中能直接观察到的外观、颜色、结构或文字 | 只能描述可见内容 |
| 待确认 | 根据图片或语境推测，但用户没有确认 | 不可以，写入缺口 |
| 未获取 | 当前没有证据 | 不可以 |

价格、参数、成分、认证、功效、排名、销量、优惠、用户评价和“第一/最好/保证”等表达，必须有用户提供或可审计来源。图片不能证明图片外不可见的技术参数。涉及医疗、食品、化妆品或安全功效时，不把宣传词自动升级成事实或保证。

### 2. 多轮互动要有检查点

采用最少充分轮次，不机械凑轮数：

1. **收集轮**：一次问全字段，并请求上传产品图；用户只想做一项时同步锁定输出范围。
2. **核对轮**：逐张检查图片，记录产品主体、颜色、形状、材质外观、可读文字、品牌标识、可用裁切和不可确认项；补问会改变结果的缺口。
3. **创作前确认**：展示事实卡、卖点排序、文案口吻、视觉方向、平台比例和交付清单。用户确认后再生成正式图像或视频；用户明确要求“一次先出草稿”时，可以先做可回退草稿并标注草稿状态。
4. **反馈轮**：用户反馈后只改指定变量，复用已确认的事实卡和产品图，不重复收集。

缺少产品图时，可以先做文字文案和素材清单；不能声称已经完成保持产品外观一致的产品图或产品视频。用户拒绝某字段时最多追问一次，之后写“待补充”，不使用占位事实冒充已知信息。

### 3. 推荐语与朋友圈文案

推荐语使用“场景/痛点钩子 → 产品卖点 → 用户利益或行动建议”的链条，优先写功能如何改变具体场景，避免空泛形容词。

朋友圈短文：

- 开头让读者知道产品解决什么具体处境。
- 中段只展开 2—4 个已确认卖点，并用生活化语言说明利益。
- 结尾使用与用户要求一致的行动语；没有授权时不代替用户私聊、发券、发布或承诺效果。
- 事实不足时宁可少写，也不补造价格、优惠、参数、案例或评价。

需要多个版本时，改变角度或语气，不重复堆砌同一组卖点。具体结构见 references/output-patterns.md。

### 4. 根据产品图重构电商卖点图解

图像工作必须以用户提供的产品图或明确授权的官方资产为参考：

1. 先为每个画面写图解卡：卖点、事实依据、画面主体、可见动作/细节、允许出现的文字、比例。
2. 默认一图一卖点；卖点多时拆图，不把 5 个互不相关的卖点压在一张海报里。
3. 保持产品身份稳定：轮廓、颜色、按键/接口、包装结构、Logo 位置和可读文字不能无故改变。出现身份漂移时拒绝交付并重做 Prompt 或换编辑方式。
4. 视觉风格由产品调性、平台和用户偏好决定。用户未指定时，优先清晰、可读、能快速理解卖点的电商图解版式，不强制所有产品使用深色大片风或引导线卡片。
5. 图上标题、数字、单位和卖点文字只使用事实卡中的允许文本；生成后逐字回读，错字、错数、重复标签、伪造 Logo 或暗示性背书都视为失败。
6. 工具支持一次成型文字时，把标题和画面一起生成/编辑；工具不能稳定生成可读文字时，交付文字安全区和可执行 Prompt，并明确文字尚未验证，不把低清候选当正式图。

如需要大象品牌的小黑象、手绘解释图或信息图，才按需加载相关 Skill；不要把品牌视觉默认强加给一般产品展示任务。

### 5. 15 秒卖点视频

默认竖屏 9:16、精确 15 秒；用户指定横屏或其他平台时服从指定比例。可按产品特性调整镜头，但必须保留卖点浓缩逻辑：

| 时间 | 任务 | 最低要求 |
|---|---|---|
| 0—2 秒 | 钩子 | 产品主体或明确痛点尽快出现 |
| 2—5 秒 | 使用处境 | 让观众知道为什么需要它 |
| 5—10 秒 | 卖点证明 | 只讲 2—3 个最高优先级卖点，画面必须能承载 |
| 10—13 秒 | 结果/场景 | 展示已确认的使用利益，不编造效果 |
| 13—15 秒 | 收束 | 产品名、核心记忆点和用户指定的行动语 |

输出字段至少包括：镜号、起止时间、画面、产品素材来源、口播、屏幕字幕、转场/动作、音频备注和事实依据。每句字幕短到能在手机上读完，避免把完整说明书塞进 15 秒。

有产品图时，优先使用图生视频或以产品图为参考的当前视频能力；没有产品图时只生成概念性分镜，不把文生视频结果描述成真实产品展示。视频生成失败、不可用或返回状态不明确时，交付脚本和缺口表，不声称已有成片。

## 能力路由

按需加载资源，不把所有下游 Skill 内容复制进本 Skill：

- 产品字段与第一轮问题：references/collection-template.md
- 推荐语、朋友圈文案、图解 Prompt、15 秒分镜：references/output-patterns.md
- 真实产品图编辑、图像一致性和视频工具失败：GOTCHAS.md
- 复杂视频编导或渲染：按需调用 video-director、workflow-video-production。
- 信息图或大象品牌配图：只有用户明确需要时才调用 baoyu-infographic、xiaohei-elephant-scenes。

当前运行时可能提供图像查看、图像生成/编辑、视频生成或文件展示能力，也可能不提供。以实际可调用能力为准；不要把某个 Agent 的工具名写成所有 Agent 都存在的前提。

## 标准交付包

交付时按用户选择输出，建议使用以下顺序：

1. **状态**：DRAFT、READY_FOR_REVIEW、PASS 或 BLOCKED。
2. **产品事实卡**：已提供、图中可见、待确认、未获取。
3. **推荐语与朋友圈短文**：标出引用的卖点和需要用户补充的字段。
4. **图片清单**：每张图的用途、比例、画面说明、允许文本、原图引用和 QA 结果。
5. **15 秒视频包**：分镜表、口播、字幕、素材清单、视频 Prompt；如有真实输出文件，再单独列出文件路径。
6. **待裁决项**：只列会影响交付的缺口。
7. **迭代入口**：让用户可以指定“改第几张图/哪句文案/哪个时间段”，不重新从零开始。

文件落盘时遵循当前 Agent 和项目的既有目录规则；不要为了本流程新建平行事实源。正式图片、视频和项目资产按共享工作区的项目归档规则保存。

## Common Pitfalls

1. **把豆包专用工具名当成通用工具。** 处理：使用当前 Agent 实际可用的图像/视频能力；不可用时降级为 Prompt、分镜和缺口表。
2. **用产品图推断看不见的参数。** 处理：图片只证明可见外观；规格、成分、认证和功效回到用户事实或可靠来源。
3. **图像生成后产品变形或串入其他参考图。** 处理：一次编辑只绑定必要的产品参考图，逐张做轮廓、颜色、Logo、文字和数字回读。
4. **把脚本交付当成视频成片。** 处理：把脚本、生成任务、预览和最终文件分开标状态；没有可回读的文件就写“未生成/未验证”。
5. **卖点、文字和比例一次堆满。** 处理：一图一卖点，15 秒只保留最高优先级的 2—3 个卖点，优先可读性和产品识别。
6. **把草稿或候选图直接当成可发布内容。** 处理：经过事实卡、用户检查点和图像/视频 QA 后再标记 PASS；发布仍需单独授权。

## Verification Checklist

- [ ] 触发词覆盖用户的产品介绍、产品说明、产品视频、产品图文、推荐语和朋友圈分享表达。
- [ ] 事实卡已区分用户提供、图片可见、待确认和未获取。
- [ ] 产品图、文案、图上文字和视频字幕没有引入未经证实的参数、价格、功效、案例或评价。
- [ ] 图解图确实以用户产品图为参考，产品身份和允许文本已回读。
- [ ] 15 秒脚本总时长为 15 秒，卖点不超过可读承载范围。
- [ ] 没有把不可用工具、候选图或脚本描述成已完成的成片。
- [ ] 交付状态、待裁决项和用户下一轮可修改的变量已列明。
- [ ] 只修改了用户要求的内容；没有默认发布、发送或外部同步。
